# Local development — copy to .env and adjust as needed
VITE_API_URL=http://localhost:3001
DOMX_API_URL=http://localhost:3001
DOMX_ELECTRON_SERVICE_KEY=change-me-in-production

# Production builds use .env.production (https://api.low7labs.cloud)
